{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch RPC",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "envFile": "${workspaceFolder}/.env",
      "program": "${workspaceFolder}/cmd/rpc/main.go",
      "cwd": "${workspaceFolder}"
    }
  ]
}